# Database password
ISPO_DB_PASSWORD=change-me-in-production

# JWT secret for signing tokens (min 32 characters, use: openssl rand -hex 32)
ISPO_AUTH_JWT_SECRET=change-me-in-production

# Admin API key for emergency access
ISPO_ADMIN_API_KEY=change-me-in-production
